{% set name = "questplus" %}
{% set version = "2019.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 434a3b9e037a97c8ff4537f35a0a5b5af40188e8d09a6caaf581a1166564b54d

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6
    - xarray
    - scipy
    - json_tricks

test:
  imports:
    - questplus

about:
  home: https://github.com/hoechenberger/questplus
  license: GPL-3.0
  license_family: GPL
  license_file: LICENSE
  summary: 'A QUEST+ implementation in Python'
  description: |
    This is a simple implementation of the QUEST+ algorithm in Python.

extra:
  recipe-maintainers:
    - hoechenberger
